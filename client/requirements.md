## Packages
framer-motion | For smooth page transitions and micro-interactions
recharts | For dashboard analytics and charts
date-fns | For date formatting and calendar logic
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Authentication uses Replit Auth (already setup).
Frontend must handle role-based routing (student, faculty, staff, admin).
Colors should reflect LUMS identity (Green, Gold, White) but modern.
